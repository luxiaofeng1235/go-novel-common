package api_routes

import (
	"github.com/gin-gonic/gin"
)

func initPayRoutes(r *gin.RouterGroup) gin.IRoutes {
	//payApi := new(api.Pay)
	//pay := r.Group("/pay")
	//{
	//	pay.POST("/getVipRecords", payApi.GetVipRecord)
	//}
	return r
}
